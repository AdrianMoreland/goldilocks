# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# NOT dist/dist-ssr: Railway's Railpack builder uses gitignore rules to
# decide what makes it from the build stage into the runtime image, so
# ignoring this hid the actual `vite build` output from the deployed
# container.
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
